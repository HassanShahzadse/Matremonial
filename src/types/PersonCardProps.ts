interface PersonCardProps {
  name: string;
  role: string;
  imageUrl: string;
  para: string;
}
export default PersonCardProps;
// Assuming footerData is an array of objects
const footerData: { [key: string]: any }[] = [
  {
    /* data */
  },
];
